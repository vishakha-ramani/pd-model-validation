# Python
__pycache__/
*.py[cod]
.pytest_cache/
.venv/
venv/

# Figures and reports regenerated by the scripts. The committed *.reference.png
# files are the versions to diff against, so the generated copies stay untracked.
figures/titer_modeb.png
figures/ttft_parity.png
figures/compute_term_verification.json
figures/ttft_error_decomposition.json

# Raw captures pulled from the cluster into the repository root by the runbooks.
# The admission trajectory is ~1.1 GB and gets regenerated by
# calibration/admission/run.md rather than stored here. These patterns are
# anchored, because calibration/results/modeb/meta.json IS vendored and required.
/trajectory.jsonl
/admission_events.jsonl
/meta.json

# Local schema-v2 admission smoke captures. The uncompressed trajectories are
# hundreds of MiB and are exploratory artifacts, not the validated coefficient
# datasets published under calibration/results/.
/calibration/admission/results-v2-smoke/
